<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="OriginalListView" TargetType="ListView" BasedOn="{StaticResource MaterialDesignListView}"/>

    <Style TargetType="ListView" BasedOn="{StaticResource OriginalListView}">
        <Setter Property="Background" Value="{DynamicResource ListView}"/>
        <Setter Property="Height" Value="100"/>
    </Style>

    <Style x:Key="MainListView" TargetType="ListView" BasedOn="{StaticResource OriginalListView}">
        <Setter Property="Height" Value="400"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Background" Value="{DynamicResource ListView}"/>
        
    </Style>

    <Style x:Key="AppoitmentsListView" TargetType="ListView" BasedOn="{StaticResource OriginalListView}">
        <Setter Property="Height" Value="200"/>
        <Setter Property="Background" Value="{DynamicResource ListView}"/>
        <Setter Property="Width" Value="680"/>
    </Style>

</ResourceDictionary>