<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="OriginalTextBlock" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
        <Setter Property="Foreground"  Value="{StaticResource TextBlockColor}"/>
    </Style>

    <Style x:Key="PreviousAppointmentTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="Count" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="40"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ListPacient" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="Errors2" TargetType="TextBlock" >
        <Setter Property="Foreground" Value="Red"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style x:Key="NameTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="Margin" Value="5,0"/>
    </Style>

    <Style x:Key="SurnameTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}"></Style>
    <Style x:Key="LastNameTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}"/>
    <Style x:Key="SpecialityTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}"/>

    <Style x:Key="TextBlockPacientName" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="TextBlockPacientSurname" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="5,0,5,0"/>
    </Style>

    <Style x:Key="TextBlockPacientLastName" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}"/>

    <Style x:Key="TextBlockPacientBirthday" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="5,0,0,0"/>
    </Style>

    <Style x:Key="PriemTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="PacientTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="NamePAcient" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="SurnamePAcient" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="LastNamePAcient" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,20,0"/>
    </Style>

    <Style x:Key="PhoneTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="AppoitmentsStoryTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style x:Key="Data" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="Data2" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,15,0"/>
    </Style>

    <Style x:Key="Diagnosis" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="DiagnosisTextBlock" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Width" Value="200"/>
    </Style>

    <Style x:Key="Recomendations" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,5,0"/>
    </Style>

    <Style x:Key="Recomendations2" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Width" Value="300"/>
    </Style>

    <Style x:Key="Diagnosis2" TargetType="TextBlock" BasedOn="{StaticResource OriginalTextBlock}">
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

</ResourceDictionary>